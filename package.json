{
  "name": "beacon-health",
  "version": "0.1.0",
  "description": "Clean-room TypeScript demo tool for synthetic wellness operations summaries.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/src/index.js",
    "test": "npm run build && node --test dist/test/validation.js"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "typescript": "^5.5.3"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
