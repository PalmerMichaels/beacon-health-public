{
  "name": "beacon-health",
  "version": "0.1.0",
  "private": true,
  "description": "Clean-room TypeScript CLI that summarizes synthetic operations queues.",
  "type": "module",
  "scripts": {
    "start": "node src/index.ts",
    "test": "node --test tests/*.test.ts",
    "validate": "node scripts/validate.ts"
  },
  "engines": {
    "node": ">=22"
  }
}
